/**
 * 49. Group Anagrams
 * https://leetcode.com/problems/group-anagrams/
 * 
 * Given array of strings, group anagrams together.
 * 
 * Hint: Use sorted characters as key - all anagrams have same sorted form
 */
export function groupAnagrams(strs) {
  // Step 1: Create a map: key = sorted string, value = array of anagrams
  // Step 2: For each string, sort its characters to create key
  // Step 3: Push string to appropriate bucket
  // Step 4: Return Object.values(map)
  
  // Your code here
}
