/**
 * 1. Two Sum
 * https://leetcode.com/problems/two-sum/
 * 
 * Given array of integers and target, return indices of two numbers that add to target.
 * 
 * Hint: Use a Map to store value → index, check for complement
 */
export function twoSum(nums, target) {
  // Step 1: Create a Map to store number → index
  // Step 2: For each element, calculate complement = target - current
  // Step 3: If complement exists in Map, return [complementIndex, currentIndex]
  // Step 4: Otherwise, store current number → index
  
  // Your code here
}
