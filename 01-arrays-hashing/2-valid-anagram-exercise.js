/**
 * 242. Valid Anagram
 * https://leetcode.com/problems/valid-anagram/
 * 
 * Given two strings s and t, return true if t is an anagram of s.
 * 
 * Hint: Count character frequencies in first string, verify against second
 */
export function isAnagram(s, t) {
  // Step 1: Early exit if lengths differ
  // Step 2: Create frequency count object for s
  // Step 3: Decrement counts while iterating t
  // Step 4: If any count goes negative, not an anagram
  
  // Your code here
}
