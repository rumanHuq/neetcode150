/**
 * 217. Contains Duplicate
 * https://leetcode.com/problems/contains-duplicate/
 * 
 * Given an integer array nums, return true if any value appears at least twice in the array.
 * 
 * Hint: Use a Set to track seen elements
 */
export function containsDuplicate(nums) {
  // Step 1: Create a Set to store seen elements
  // Step 2: Iterate through array
  // Step 3: If element already in Set, return true
  // Step 4: Otherwise, add to Set
  // Step 5: Return false if loop completes
  
  // Your code here
}
