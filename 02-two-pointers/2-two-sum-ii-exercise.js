/**
 * 167. Two Sum II - Input Array Is Sorted
 * https://leetcode.com/problems/two-sum-ii-input-array-is-sorted/
 * 
 * Given a 1-indexed array of integers numbers that is already sorted in non-decreasing order, find two numbers such that they add up to a specific target number.
 * 
 * Hint: Since array is sorted, use two pointers - move based on whether sum is < or > target
 */
export function twoSum(numbers, target) {
  // Step 1: Initialize left pointer at start, right pointer at end
  // Step 2: Calculate sum, check if equals target
  // Step 3: If sum < target, move left pointer right (need bigger)
  // Step 4: If sum > target, move right pointer left (need smaller)
  // Step 5: Return [left+1, right+1] (1-indexed)
  
  // Your code here
}
