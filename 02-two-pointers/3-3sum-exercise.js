/**
 * 15. 3Sum
 * https://leetcode.com/problems/3sum/
 * 
 * Given an integer array nums, return all the triplets [nums[i], nums[j], nums[k]] such that i != j, i != k, and j != k, and nums[i] + nums[j] + nums[k] == 0.
 * 
 * Hint: Sort array, then use two pointers for remaining two numbers
 */
export function threeSum(nums) {
  // Step 1: Sort the array
  // Step 2: For each element (as first number), use two pointers
  // Step 3: Skip duplicates to avoid repeated triplets
  // Step 4: Find pairs that sum to -nums[i]
  
  // Your code here
}
