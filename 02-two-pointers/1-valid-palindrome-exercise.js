/**
 * 125. Valid Palindrome
 * https://leetcode.com/problems/valid-palindrome/
 * 
 * A phrase is a palindrome if, after converting all uppercase letters into lowercase letters and removing all non-alphanumeric characters, it reads the same forward and backward.
 * 
 * Hint: Use two pointers - one from start, one from end
 */
export function isPalindrome(s) {
  // Step 1: Clean the string (lowercase, remove non-alphanumeric)
  // Step 2: Use two pointers to compare characters from both ends
  // Step 3: Return false if any characters don't match
  
  // Your code here
}
